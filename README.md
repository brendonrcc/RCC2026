# Mega da Virada RCC - 2026

Este é um projeto **React + Vite**. Para que ele funcione, você precisa rodar um servidor local. Não é possível abrir o arquivo `index.html` diretamente.

## 🚀 Como Rodar no seu PC

### 1. Instale o Node.js
Se ainda não tiver, baixe e instale a versão "LTS" do [Node.js](https://nodejs.org/).

### 2. Prepare o Projeto
Abra o terminal (Prompt de Comando ou PowerShell) na pasta onde você extraiu os arquivos.

1. Instale as dependências:
   ```bash
   npm install
   ```

2. Inicie o site:
   ```bash
   npm run dev
   ```

3. O terminal mostrará um link (ex: `http://localhost:5173`). Clique nele ou copie e cole no seu navegador.

## 🌐 Como Colocar na Internet (Grátis)

A maneira mais fácil é usar a **Vercel**:

1. Crie uma conta em [vercel.com](https://vercel.com).
2. Instale o Vercel CLI no seu computador: `npm i -g vercel`
3. No terminal, dentro da pasta do projeto, digite: `vercel`
4. Vá apertando `Enter` para todas as perguntas.
5. Em instantes, o site estará no ar!

---

**Nota:** Este projeto não utiliza mais a API Key do Google Gemini, funcionando de forma 100% independente.